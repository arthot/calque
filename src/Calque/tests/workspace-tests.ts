import assert = require('power-assert')
import Workspace = require('../core/Workspace')

describe("Workspace", () => {
    context("input()", () => {

        it("should split to lines", () => {

        });
    });
});